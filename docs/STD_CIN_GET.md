### [return main page](../README.md) || Topic. Function std::cin.get();

**std::cin.get();** - эта функция нужна чтобы не закрывалось окно, файл с раширение`exe`. То есть другими словами когда мы кликаем на exe, чтобы сразу не закрывалось окно, а ждала когда мы нажмём enter.

### Extern links :
* link 1
